{
  "framework": "nextjs",
  "buildCommand": "pnpm verify && pnpm build",
  "installCommand": "corepack enable && pnpm install",
  "outputDirectory": ".next",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}
